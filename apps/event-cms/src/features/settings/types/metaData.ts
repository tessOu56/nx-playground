import { createMetaDataConfig } from '../../../lib';

export const settingsMetaData = createMetaDataConfig({
  title: 'Settings - NX Playground Console',
  description: 'NX Playground Console Settings',
  keywords: ['settings', 'nx-playground', 'console'],
});
