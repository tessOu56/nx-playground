import { createMetaDataConfig } from '../../../lib';

export const dashboardMetaData = createMetaDataConfig({
  title: 'Dashboard - NX Playground Console',
  description: 'NX Playground Console Dashboard',
  keywords: ['dashboard', 'nx-playground', 'console'],
});
