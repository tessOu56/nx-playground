import { createMetaDataConfig } from '../../lib';

export const usersMetaData = createMetaDataConfig({
  title: 'Users - NX Playground Console',
  description: 'Manage users in NX Playground Console',
  keywords: ['users', 'management', 'nx-playground'],
});
